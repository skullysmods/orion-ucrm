name: Integration

on:
  push:
    branches:
      - develop
      - main

jobs:
  tests-and-quality:
    permissions:
      security-events: write
      actions: read
      contents: read
    uses: ./.github/workflows/01-1_Tests.yml
    secrets: inherit